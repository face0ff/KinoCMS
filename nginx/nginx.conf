upstream kinoCMS {
    server web:8000;
}

server {

    listen 80;
    client_max_body_size 10M;
    location / {
        proxy_pass http://kinoCMS;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }

    location /static/ {
        alias /kinoCMS/static/;
    }

    location /media/ {
        alias /kinoCMS/media/;
    }

}